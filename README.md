# Tamar React App

Migrated from the approved `code_artifact (1).html` single-file React application into a Vite React project.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The original HTML file remains outside this folder and is not required at runtime.