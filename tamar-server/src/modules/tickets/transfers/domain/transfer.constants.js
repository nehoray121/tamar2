const TRANSFER_STATUSES = Object.freeze({
    PENDING_ACCEPTANCE: 'PENDING_ACCEPTANCE',
    ACCEPTED: 'ACCEPTED',
    CANCELLED: 'CANCELLED'
});
const TRANSFER_STATUS_VALUES = Object.freeze(Object.values(TRANSFER_STATUSES));

const TRANSFER_DIRECTIONS = Object.freeze({ INCOMING: 'INCOMING', OUTGOING: 'OUTGOING', ALL: 'ALL' });
const TRANSFER_DIRECTION_VALUES = Object.freeze(Object.values(TRANSFER_DIRECTIONS));

const EXTERNAL_TRANSFER_STATES = Object.freeze({
    PENDING: 'PENDING', PROCESSING: 'PROCESSING', DONE: 'DONE', CANCELLED: 'CANCELLED'
});
const EXTERNAL_TRANSFER_STATE_VALUES = Object.freeze(Object.values(EXTERNAL_TRANSFER_STATES));

const TRANSFER_SORT_FIELDS = Object.freeze([
    'initiatedAt', 'acceptedAt', 'cancelledAt', 'updatedAt', 'ticketNumber', 'sequence'
]);

module.exports = {
    TRANSFER_STATUSES,
    TRANSFER_STATUS_VALUES,
    TRANSFER_DIRECTIONS,
    TRANSFER_DIRECTION_VALUES,
    EXTERNAL_TRANSFER_STATES,
    EXTERNAL_TRANSFER_STATE_VALUES,
    TRANSFER_SORT_FIELDS
};
